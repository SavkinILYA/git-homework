# git-homework
Курс DevOps от Netology 2026
Показываю навыки владения гит


## .gitignore

- Игнорируются все локальные директории `.terraform/*` (там кэш провайдеров)
- Файлы состояния `*.tfstate`, `*.tfstate.*`
- Логи падений `crash.log`
- Файлы с переменными `*.tfvars`, `*.tfvars.json` (часто содержат секреты)
- Переопределения `override.tf` и подобные
